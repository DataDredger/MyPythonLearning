theCount = [1, 2, 3, 4, 5]
fruits = ['apples', 'oranges', 'pears', 'apricots']
change = [1, 'pennies', 2, 'dimes', 3, 'quarters']

# lets start to rock it out with some for each loops
for i in theCount:
    print(f"we are looking at element{i}")

# can we build a list? yes we can!
newList = []
for i in range(0, 6):
    print(f"adding element {i} to the list")
    newList.append(0, 6)

# time to check out what we just built
print(f"element was{1}")
